@wines.map do |wine|
  {
    id: wine.id,
    name: wine.name,
    producer: wine.producer,
    vintage: wine.vintage,
    blend: wine.blend,
    price: wine.price,
    friendly_created_at: wine.friendly_created_at
  }
end
