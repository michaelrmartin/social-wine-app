@users.map do |user|
  {
    id: user.id,
    name: user.name,
    wines: user.wines,
    user_wines: user.user_wines,
    location: user.location,
    bio: user.bio,
    photo_url: user.photo_url,
    phone: user.phone
  }
end
